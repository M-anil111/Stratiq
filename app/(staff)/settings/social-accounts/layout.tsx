import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Social Accounts | Stratiq',
}

export default function SocialAccountsLayout({ children }: { children: React.ReactNode }) {
  return children
}
