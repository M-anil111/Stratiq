import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Publishing Schedule | Stratiq',
}

export default function SocialScheduleLayout({ children }: { children: React.ReactNode }) {
  return children
}
